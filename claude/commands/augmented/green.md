# /aug:green - 최소 구현 (GREEN 상태)

TDD 사이클의 두 번째 단계: 테스트를 통과시키는 최소한의 코드만 작성합니다.

## 워크플로우

1. **RED 상태 확인**
   - 마지막 테스트 실패 출력 확인
   - 어떤 테스트를 통과시켜야 하는지 명확히 파악

2. **구현 위치 식별**
   - 테스트가 요구하는 함수/클래스 위치 찾기
   - 없으면 새로 생성

3. **최소 구현 작성**
   - **테스트를 통과시키는 데 필요한 코드만** 작성
   - 엣지 케이스 처리 금지 (테스트에 없으면 안 함)
   - "나중을 위한" 코드 금지
   - 하드코딩도 OK (테스트 통과하면 됨)

4. **테스트 실행 - 통과 확인**
   - `npm test` 실행
   - 새 테스트가 통과하는지 확인 (GREEN 상태)

5. **전체 테스트 실행**
   - 다른 테스트가 깨지지 않았는지 확인
   - 회귀(regression) 방지

6. **다음 단계 제안**
   - `/aug:refactor` - 코드 정리가 필요하면
   - `/aug:commit-behavioral` - 바로 커밋하려면

## 제약 조건

- ❌ "제대로 구현"하려는 충동 억제
- ❌ 테스트에 없는 기능 추가 금지
- ❌ 엣지 케이스 미리 처리 금지
- ✅ 진짜 최소한만 작성 (심플한 게 최고)
- ✅ 테스트 통과가 유일한 목표

## 예시

```typescript
// 테스트
it('should return 11 when input is 10 with 10% tax rate', () => {
  expect(calculateTax(10, 0.1)).toBe(11);
});

// ✅ 좋은 예: 최소 구현
function calculateTax(amount: number, rate: number): number {
  return amount + amount * rate;
}

// ❌ 나쁜 예: 과도한 구현
function calculateTax(
  amount: number,
  rate: number,
  options?: { round?: boolean; min?: number; max?: number } // 테스트에 없음!
): number {
  if (amount < 0) throw new Error('Negative amount'); // 테스트에 없음!
  const tax = amount * rate;
  const total = amount + tax;
  return options?.round ? Math.round(total) : total; // 테스트에 없음!
}

// 😱 더 나쁜 예: 하드코딩 (하지만 이것도 OK!)
function calculateTax(amount: number, rate: number): number {
  return 11; // 테스트는 통과함! 다음 테스트에서 일반화하면 됨
}
```

## Kent Beck의 원칙

> "테스트를 통과시키기 위한 충분한 코드만 작성하라 - 그 이상은 안 된다"

- 단순한 솔루션이 복잡한 것보다 낫다
- 다음 테스트가 일반화를 강제할 것이다
- YAGNI (You Aren't Gonna Need It)
- 하드코딩 → 명백한 구현 → 삼각측량 순으로 발전

## 주의사항

더 많은 기능이 필요하다고 느낀다면:
1. 그건 다음 `/aug:red`에서 테스트로 작성하세요
2. 지금은 현재 테스트만 통과시키세요
3. 테스트가 요구사항을 정의합니다
